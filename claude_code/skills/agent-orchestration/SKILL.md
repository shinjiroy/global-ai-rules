---
name: agent-orchestration
description: 上位・高価格モデル(Fable等)がオーケストレータとなり、実装やレビューをサブエージェント(別モデル)に委任してタスクを進める時の方針。タスクをサブエージェントに分担させたい/複数モデルで実装・レビューを回したい/GOAL.mdを/goalで実行させる/どのタスクにどのモデルを割り当てるか決めたい/マイルストーンごとに実装→レビュー→修正を回したい、といった時に必ず使う。「オーケストレーション」「サブエージェントに任せる」「実行セッション」「/goalを回す」等でも発動する。単一セッションで自分で手を動かす通常作業には使わない。
---

# サブエージェント・オーケストレーション

## 原理: 難しい判断は計画に前倒しする

高価で難しい統括判断(何を作るか・どう分解するか・どの設計を採るか)は上位モデルとの計画セッションで済ませ、GOAL.md+検証コードに外部化しておく(→ `goal-authoring` スキル)。それが済めば実行は安価なオーケストレーションで回る。**計画に投資するほど実行が安くなる**。この非対称を前提に以下を運用する。

## モデルの割り当て

- **実装(ファイル読み書き・テスト実行・反復)は安価で有能なモデル(Sonnet等)に委任する**。トークン消費が最も重い層で、上位モデルを使っても限界的な品質向上が費用に見合わない
- **レビュー・仕様/品質の分析は上位モデル(Opus等)に委任する**。バグ発見のROIが高く投資に見合う
- **レビューは実装したエージェントとは別のモデル/エージェントに行わせる**。自作の自己レビューは弱いため独立性を確保する
- **どのモデルでオーケストレーションするか(最上位モデルを使うべきか)の判断基準は `references/judgment.md` を読む**

## オーケストレータのコンテキストを汚さない(最重要)

最上位モデルのコンテキストは最も高価。低価値なトークンを流し込まないこと。

- **長時間サブエージェントを `TaskOutput` の block=true でポーリングしない**。完了通知が自動で届くのでそれに任せる
- **サブエージェントの生の会話トランスクリプト(JSONL等)をオーケストレータのコンテキストに取り込まない**。エージェントの最終報告だけを読む
- オーケストレータの仕事は**タスク分解・タスク仕様の作成・受け入れ可否の判断・最終検証**に絞る。実装の手を自分で動かさない

## サブエージェントの使い回し

- 密結合な逐次作業は同一エージェントを `SendMessage` で使い回すと、コンテキスト/キャッシュを引き継げて効率的
- ただしコンテキストは単調増加しキャッシュ読み込みコストが膨らむ。マイルストーンの節目でエージェントを切るか判断する

## 実行ループ(1マイルストーンあたり)

1. オーケストレータがタスク仕様(GOAL.mdの該当マイルストーン + 制約 + 受け入れ基準)を書き、実装エージェント(Sonnet等)へ委任
2. 実装完了 → レビューエージェント(Opus等・実装者とは別)へ委任
3. 指摘を実装エージェントへ差し戻して修正
4. 検証コード(テスト・スモーク)で受け入れ基準を機械的に確認 → 次のマイルストーンへ

各ステップでオーケストレータはエージェントの最終報告だけ読み、コンテキストを軽く保つ。
